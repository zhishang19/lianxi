# 多源记忆数据包 - 全量文件说明

> 本文档按 D1~D6 六天任务流程,逐目录说明每个文件的用途、数据格式、脏数据特征与清洗产出。

---

## 数据流转全景

```
D1 观察(看题)                          raw/d1/
  │  理解"什么是脏数据"
  ▼
D2 合并(多源 JSON → merged.jsonl)      raw/d2/
  │  user_behavior.json + tool_result.json → 合并去噪
  ▼
D3 清洗(CSV 对话 → 干净 CSV)            raw/d3/
  │  chat_sessions_dirty.csv → 脱敏/去重/标准化
  ▼
D4 多源(5 格式 → 4 类 JSON + 报告)     raw/d4/
  │  JSONL/TXT/CSV/YAML/JSONL → chats/knowledge/preferences/tools
  ▼
D5 抽取(事件 → 最终记忆 + 冲突报告)    raw/d5/
  │  events + snapshots → merged_memories (action/reason/evidence)
  ▼
D6 评测(记忆 → 10 题回答 → 质量报告)   raw/d6/
     eval_e2e.py → eval_report.md (6 项指标全部达标)
```

---

## D1: 观察 demo (学生入门)

**任务目标**: 观察脏数据长什么样,写出 5 条清洗假设,不要求写代码。

**分发对象**: 学生

### 文件清单

| 文件 | 角色 | 格式 | 行数 | 说明 |
|------|------|------|------|------|
| [observe_demo.txt](raw/d1/observe_demo.txt) | 输入(脏数据样例) | TXT | 5 行 | 5 条典型脏数据,覆盖 5 类噪声场景 |
| [zangsj.md](raw/d1/zangsj.md) | 输出(学生作业) | MD | 5 条 | 学生写的清洗假设 |
| [README.md](raw/d1/README.md) | 说明文档 | MD | — | 练习目标与要求 |

### observe_demo.txt — 脏数据特征详解

| # | 内容 | 噪声类型 | 清洗方向 |
|---|------|---------|---------|
| 1 | `嗯……帮我把月报导出成 PDF，简洁一点！！！` | 口水词(`嗯……`)+感叹号堆叠 | 去除口水词和多余标点 |
| 2 | `exported: /home/u001/月报.pdf <!-- cached -->` | HTML 缓存标签 | 清除缓存标记 |
| 3 | `刚才说错了，这次要详细版，不代表以后都这样。` | 临时指令 vs 长期偏好 | 标记 scope=short,不覆盖长期 |
| 4 | `我的邮箱是 demo@example.com，别记下来。` | 隐私信息+forget 指令 | 脱敏邮箱,执行 forget |
| 5 | `麒麟系统驱动更新入口可能与版本有关，需要记录证据来源。` | 证据缺失 | 保留但标记 needs_review |

### zangsj.md — 学生清洗假设

学生识别出 5 类处理规则:
1. 口水词/感叹号 → 清洗
2. 缓存标签 `<!-- cached -->` → 清洗
3. "不代表以后都这样" → 保留(临时指令)
4. 邮箱/手机号 → 隐私脱敏
5. "需要记录证据来源" → 保留但需人工检查

---

## D2: 合并练习 (多源 JSON 合并)

**任务目标**: 编写 `merge_day2.py`,将 `user_behavior.json`(用户行为) 和 `tool_result.json`(工具结果) 合并为统一的 `merged.jsonl`。

**分发对象**: 学生

### 文件清单

| 文件 | 角色 | 格式 | 记录数 | 说明 |
|------|------|------|--------|------|
| [user_behavior.json](raw/d2/user_behavior.json) | 输入(用户行为) | JSON Array | 6 条 | 用户聊天/点击/语音记录 |
| [tool_result.json](raw/d2/tool_result.json) | 输入(工具结果) | JSON Object | 3 条 | 工具执行结果(嵌套在 results 数组中) |
| [merge_day2.py](raw/d2/merge_day2.py) | **脚本**(核心产出) | Python | — | 合并+去噪引擎 |
| [merge_day2.ps1](raw/d2/merge_day2.ps1) | 脚本(PS 版) | PowerShell | — | 同上逻辑的 PS 实现 |
| [merged.jsonl](raw/d2/merged.jsonl) | **输出**(合并后) | JSONL | 8 条 | 统一格式干净记录 |

### 输入数据脏特征

**user_behavior.json** (6 条原始):
- uid 大小写不一致: `U001` vs `u001` vs `U002`
- 字段名不统一: 有的用 `uid`/`time`/`action`/`content`,有的用 `user_id`/`timestamp`/`type`/`text`
- 时间格式混乱: `2026/6/2 09:15` / `2026-06-02T09:16:00` / `2026年6月2日 10:00` / 空
- content 含噪声: 口水词(`嗯嗯…那个…`)、全角空格、多余标点(`!!!`)、重复行(第 1/4 条完全相同)
- 类型混杂: `chat` / `click` / `voice`

**tool_result.json** (3 条原始):
- 嵌套结构: 外层 `{results: [...]}`,内层每条有 `trace_id/tool/status/output/latency_ms`
- latency_ms 类型不一: 数字 `890` vs 字符串 `"312"`
- output 含噪声: 缓存标签(`<!-- cached -->`)、乱码路径(`/tmp/???.docx`)、口水词(`呃呃呃`)
- status 值: `success` / `fail`

### merge_day2.py 处理逻辑

```
输入: user_behavior.json + tool_result.json
  ↓
1. 字段归一化: uid→小写, action/type→统一为 type, text/content→统一为 content
2. 时间标准化: 所有格式 → ISO 8601 (2026-06-02T09:15:00); 无效时间标 time_invalid=true
3. 来源标记: source = "user_behavior" 或 "tool_result"
4. 工具特有字段: tool_status, tool_latency_ms, trace_id
5. 去重: 相同(uid+time+content)的行只保留一条, dup_count 记录原始出现次数
  ↓
输出: merged.jsonl (8 条, 每条含 source/uid/time/time_invalid/type/content/[tool字段]/dup_count)
```

### merged.jsonl 输出示例

```json
{"source":"user_behavior","uid":"u001","time":"2026-06-02T09:15:00","time_invalid":false,"type":"chat","content":"…那个…帮我把 文档 导出成 PDF 吧！","dup_count":2}
{"source":"tool_result","uid":null,"time":null,"time_invalid":true,"type":"file_export","content":"ERROR: path not found: /tmp/???.docx 呃呃呃","tool_status":"fail","tool_latency_ms":890,"trace_id":"T-1002","dup_count":1}
```

---

## D3: 基础验收 (CSV 对话清洗)

**任务目标**: 编写 `clean_basic.py`,将 `chat_sessions_dirty.csv` 清洗为干净的 CSV。

**分发对象**: 学生

### 文件清单

| 文件 | 角色 | 格式 | 行数 | 说明 |
|------|------|------|------|------|
| [chat_sessions_dirty.csv](raw/d3/chat_sessions_dirty.csv) | 输入(脏对话) | CSV | 12 行(含表头) | 4 个 session 的对话记录 |
| [clean_basic.py](raw/d3/clean_basic.py) | **脚本**(核心产出) | Python | — | CSV 清洗引擎 |
| [clean_basic.ps1](raw/d3/clean_basic.ps1) | 脚本(PS 版) | PowerShell | — | 同上逻辑的 PS 实现 |
| [chat_sessions_clean.csv](raw/d3/chat_sessions_clean.csv) | **输出**(干净 CSV) | CSV | 11 行(含表头) | 清洗后记录 |

### 输入数据脏特征 (chat_sessions_dirty.csv)

| 问题类型 | 示例 | 出现位置 |
|---------|------|---------|
| **HTML 标签** | `<b>麒麟</b>` | S001 第 1 行 |
| **口水词/重复标点** | `你好啊啊啊！！！` / `然后然后` / `嗯…` | S001/S002 多处 |
| **前后空白** | `  你好啊啊啊！！！...  ` | S001 第 1 行 |
| **时间格式混乱** | `2026/6/3 08:00` / `2026-06-03T09:00:00` / `2026年6月3日 11:00` / `2026/6/3 8:02` | 各 session 不一致 |
| **重复行** | S001 第 1 行和第 8 行完全相同 | 去重后剩 1 条 |
| **空消息** | S003 user 行 message 为空 | 标记 empty_message |
| **缺失 user_id** | S004 user 行 user_id 为空 | 标记 missing_user_id |
| **手机号明文** | `13812345678` | S004 user 行 → 脱敏为 `[PHONE]` |

### clean_basic.py 处理逻辑

```
输入: chat_sessions_dirty.csv
  ↓
1. 去除 HTML 标签 (<b>, </b> 等)
2. 压缩连续空白和标点 (啊啊啊→啊, !!!→!)
3. 去除口语填充词 (嗯…, 呃, 然后 然后)
4. 时间统一为 ISO 格式 (YYYY-MM-DD HH:mm:ss)
5. 手机号正则匹配 → 替换为 [PHONE]
6. 邮箱正则匹配 → 替换为 [EMAIL]
7. 去重 (相同 session_id+user_id+role+message)
8. 标记异常行: flags 列 (ok / empty_message / missing_user_id / phone_masked)
  ↓
输出: chat_sessions_clean.csv (新增 flags 列)
```

### chat_sessions_clean.csv 输出

```
session_id,user_id,role,message,created_at,flags
S001,U001,user,你好！我想问那个…麒麟 怎么装软件呀？,2026-06-03 08:00:00,ok
S003,U003,user,,2026-06-03 10:00:00,empty_message
S004,,user,密码忘了怎么办？？？[PHONE],2026-06-03 11:00:00,"missing_user_id,phone_masked"
```

---

## D4: 多源清洗实战 (5 种格式 → 4 类 JSON)

**任务目标**: 读取 5 个不同格式的源文件,按类型分派清洗策略,输出结构化的 4 类 JSON + 清洗报告。

**分发对象**: 学生

### 文件清单

| 文件 | 角色 | 格式 | 大小 | 说明 |
|------|------|------|------|------|
| [chat_logs_raw.jsonl](raw/d4/chat_logs_raw.jsonl) | 输入(JSONL 对话) | JSONL | 16 行 | 多轮对话日志,7 个 session |
| [knowledge_raw.txt](raw/d4/knowledge_raw.txt) | 输入(TXT 知识) | TXT | 60 行 | 知识案例文本,自定义分隔符 |
| [preferences_raw.csv](raw/d4/preferences_raw.csv) | 输入(CSV 偏好) | CSV | 13 行 | 用户偏好表,12 条偏好 |
| [config_manual.yaml](raw/d4/config_manual.yaml) | 输入(YAML 配置) | YAML | 16 行 | 管理员手动配置(默认偏好+安全规则) |
| [tool_result_raw.jsonl](raw/d4/tool_result_raw.jsonl) | 输入(JSONL 工具) | JSONL | 8 行 | 工具执行轨迹 |
| [clean_multi_source.py](raw/d4/clean_multi_source.py) | **脚本**(核心产出) | Python | — | 5 合 1 清洗引擎 |
| [chats.json](raw/d4/chats.json) | **输出**(结构化对话) | JSON | — | 按 session 分组的对话,已脱敏 |
| [knowledge.json](raw/d4/knowledge.json) | **输出**(结构化知识) | JSON | — | 6 个知识案例,错别字已修正 |
| [preferences.json](raw/d4/preferences.json) | **输出**(结构化偏好) | JSON | — | 8 个聚合偏好(uid+key 去重),冲突已标记 |
| [tools.json](raw/d4/tools.json) | **输出**(结构化工具) | JSON | — | 7 条工具记录(去重后) |
| [report.md](raw/d4/report.md) | **输出**(清洗报告) | MD | — | 统计+隐私+冲突+needs_review |

### 5 个源文件脏特征详解

#### 1. chat_logs_raw.jsonl (16 行, 7 sessions)

| 噪声类型 | 示例 | 影响 |
|---------|------|------|
| uid 大小写 | `u001` vs `U002` vs `U005` | 归一化问题 |
| 口水词 | `嗯…那个…` / `不对不对！！` / `@@@` | 内容噪声 |
| HTML/格式 | 无(但 content 有特殊符号) | — |
| 时间格式混乱 | `2026-06-04 09:00:00` / `2026/6/4 10:15` / `2026年6月4日 14:00` / `2026-06-04T17:20:00+08:00` | 标准化 |
| 重复行 | S100 第 1 行和第 9 行完全相同 | 去重 |
| **隐私泄露** | `zhangsan@example.com` (S103) / `13900001111` (S105) | 必须脱敏 |
| **forget 指令** | "别记下来啊！" (S103) / "别保存" (S105) | 执行遗忘 |
| **临时指令** | "今天这次例外…不代表以后都这样" (S104) | scope=short |
| **冲突信号** | "请用这个覆盖旧答案" (S106) | 标记 override |

Session 分布:
- **S100** (u001): 月报导出偏好变更 (简洁→详细),3 行 + 1 行重复
- **S101** (U002): emoji 禁用偏好,2 行
- **S102** (u003): 驱动更新知识(奇麟→麒麟),2 行
- **S103** (u004): 邮箱 forget,1 行
- **S104** (U005): 会议纪要偏好(长期三段式+临时 bullet),3 行
- **S105** (u006): 密码流程+手机号 forget,2 行
- **S106** (u003): 驱动入口纠正(系统更新→驱动管理器),1 行

#### 2. knowledge_raw.txt (60 行, 6 个案例)

自定义格式: `=== 案例开始 ===` ... `=== 案例结束 ===`

| 案例 | 标题 | 脏特征 |
|------|------|--------|
| #1 | 离线安装 .deb 包 | 口水词(`嗯…首先那个`/`就…呃…`)、全角空格混入、多余标点(`***重启***`) |
| #2 | 会议纪要模板 | 口水词、mask 符号(`***`)、内容不完整 |
| #3 | **垃圾行** | `<<<<<< 测试粘贴 >>>>>>` + 空标题案例 → 应删除 |
| #4 | 驱动更新入口 | 干净! 但含"新旧说法对比",需提取最新版本 |
| #5 | 敏感信息处理原则 | 干净! 安全规则文本 |
| #6 | 会义纪要偏好计忆 | **错别字**: 会义→会议, 计忆→记忆 |

#### 3. preferences_raw.csv (12 条偏好)

| 问题类型 | 示例 | 行号 |
|---------|------|------|
| uid 大小写 | `U002` vs `u002`, `U005` vs `u005` | P3/P4/P7/P8/P9 |
| **重复记录** | P1 和 P5 完全相同 (`u001/output_style/简洁/v1`) | P1, P5 |
| **uid 缺失** | P6 的 uid 为空 (`security_level`) | P6 |
| **冲突对** | P3(禁用) vs P9(允许 emoji) — 用户 vs 默认 | P3, P9 |
| **冲突对** | P11(系统更新) vs P12(驱动管理器) — 旧 vs 新知识 | P11, P12 |
| **冲突对** | P1(简洁) vs P2(详细版) — 旧 vs 新偏好 | P1, P2 |
| **冲突对** | P7(三段式) vs P8(bullet) — 长期 vs 临时 | P7, P8 |
| 错别字 | P4 含 `奇麟` (应为麒麟) | P4 |

#### 4. config_manual.yaml (管理员配置)

相对干净,但包含:
- **默认偏好与用户表达冲突**: `emoji_policy: 允许` 与 U002 的"禁用"矛盾
- 安全规则定义: 敏感模式(phone/email 正则)、forget 命令关键词列表

#### 5. tool_result_raw.jsonl (8 条工具轨迹)

| 问题类型 | 示例 | 行号 |
|---------|------|------|
| 缓存标签 | `<!--done-->` / `<!--cache-hit:false-->` | T-501, T-505 |
| 口水词 | `嗯嗯完成啦！！` / `嗯... retry? retry?` | T-502, T-506 |
| **重复行** | T-506 出现 2 次(完全相同) | 行 6, 7 |
| latency 类型混合 | 数字 `420` vs 字符串 `"640"` | T-501 vs T-505 |
| status 值不统一 | `ok` / `error` / `fail` | 各行不同 |
| **邮箱泄露** | `li@example.com` (T-505) / `zhangsan@example.com` (T-507) | T-505, T-507 |
| **forget 操作** | T-507: memory_delete 删除了 email | T-507 |
| 超时错误 | T-503: web_search timeout 3000ms | T-503 |

### clean_multi_source.py 处理逻辑

```
5 个源文件 → 按格式分派清洗器:
  ├── JSONL 清洗器 → chat_logs_raw.jsonl → chats.json
  │   └── 去噪/脱敏(uid 归一化/时间 ISO/邮箱[EMAIL]/手机号[PHONE]/去重)
  ├── TXT 清洗器  → knowledge_raw.txt → knowledge.json
  │   └── 解析 === 分隔符/去除口水词/删除垃圾行/错别字修正
  ├── CSV 清洗器  → preferences_raw.csv → preferences.json
  │   └── uid 小写/去重聚合(uid+key)/冲突标记/缺 uid 标记 needs_review
  ├── YAML 解析器 → config_manual.yaml → 合入 preferences.json
  │   └── 提取默认偏好,与用户偏好比对冲突
  └── JSONL 清洗器 → tool_result_raw.jsonl → tools.json
      └── 去重/统一 status(ok→success)/latency 转 int/去除缓存标签
```

### report.md 关键发现

| 类别 | 数量 | 详情 |
|------|------|------|
| 对话 | 15 条 / 7 session | 去重 1 条(S100) |
| 知识 | 6 个案例 | 删除 1 个垃圾行,修正 1 个错别字 |
| 偏好 | 8 个聚合 | 去重后 (12→8) |
| 工具 | 7 条 | 去重 1 条(T-506) |
| **隐私字段** | 2 处 | S103→[EMAIL], S105→[PHONE] |
| **冲突项** | 4 组 | emoji_policy(用户>默认), output_style(v2>v1), driver_update_entry(v2>v1) |
| **needs_review** | 5 条 | security_level(缺 uid)、空标题知识、错别字记忆、2 条临时指令、2 条 forget |

---

## D5: 记忆抽取与冲突合并

**任务目标**: 从多轮交互事件中抽取 4 类记忆(preference/knowledge_update/temporary_instruction/forget),完成冲突解决,输出最终记忆库。

**分发对象**: 学生

### 文件清单

| 文件 | 角色 | 格式 | 记录数 | 说明 |
|------|------|------|--------|------|
| [memory_events_raw.jsonl](raw/d5/memory_events_raw.jsonl) | 输入(原始事件) | JSONL | 14 条 | E-001~E-014 事件流 |
| [user_memory_snapshots_raw.csv](raw/d5/user_memory_snapshots_raw.csv) | 输入(用户快照) | CSV | 12 行 | 当前记忆快照(用于差异比对) |
| [conflict_candidates_raw.txt](raw/d5/conflict_candidates_raw.txt) | 输入(冲突候选) | TXT | 29 行 | 7 组冲突场景及预期处理 |
| [merge_memories.py](raw/d5/merge_memories.py) | **脚本**(核心产出) | Python | — | 记忆抽取+冲突解决引擎 |
| [merged_memories.jsonl](raw/d5/merged_memories.jsonl) | **输出**(最终记忆) | JSONL | 12 条 | 每条含 action/reason/evidence/history |
| [conflict_report.md](raw/d5/conflict_report.md) | **输出**(冲突报告) | MD | — | 决策依据+needs_review+forget 清单+快照差异表 |

### memory_events_raw.jsonl — 14 条事件详解

| event_id | uid | 类型 | 内容摘要 | 脏特征 |
|----------|-----|------|---------|--------|
| E-001 | u001 | preference | 输出详细版,带数据表格 | 干净, conf=0.92 |
| E-002 | U001 | preference | 输出简洁版 | **uid 大小写** (U001), conf=0.68, 是 E-001 的旧版本 |
| E-003 | u002 | preference | 回复不要使用 emoji | **时间格式** `2026/6/4 10:15`, **置信度** `high`(非数字) |
| E-004 | u002 | preference | 默认允许 emoji | 系统默认, conf=0.40, 与 E-003 冲突 |
| E-005 | u003 | knowledge_update | 驱动更新入口是系统更新 | **时间格式** `2026年6月4日 14:00`, conf=0.55(低置信度) |
| E-006 | u003 | knowledge_update | 驱动更新入口是驱动管理器 | 覆盖 E-005, conf=0.96(高置信度) |
| E-007 | u004 | **forget** | zhangsan@example.com | 邮箱遗忘指令, conf=1.0 |
| E-008 | u005 | preference | 会议纪要长期用三段式 | **时间格式**含时区 `+08:00`, conf=0.95 |
| E-009 | u005 | **temporary_instruction** | 今天这次用 bullet 列表 | scope=short, 不覆盖长期 |
| E-010 | u006 | knowledge | 密码重置流程 | conf=0.88 |
| E-011 | u006 | **forget** | 13900001111 | 手机号遗忘指令, conf=1.0 |
| E-012 | u007 | preference | 先结论后步骤;不要太长 | **时间为空**, conf=0.61, 与 E-013 冲突 |
| E-013 | u007 | preference | 需要**祥细**解释,每一步写原因 | **错别字** 祥细→详细, conf=0.82 |
| E-014 | u008 | knowledge | deb 安装命令 | conf=0.90 |

### user_memory_snapshots_raw.csv — 快照与事件差异

快照是系统当前存储的记忆状态,用于和事件流比对发现不一致:

| uid | key | 快照值 | 事件值 | 差异原因 |
|-----|-----|-------|--------|---------|
| u001 | output_style | 简洁/详细版(两条) | 输出详细版,带数据表格 | 快照有冗余,以事件为准 |
| u002 | emoji_policy | 允许/禁用(两条) | 回复不要使用 emoji | 快照含默认值,取用户明确值 |
| u003 | driver_update_entry | 系统更新/驱动管理器 | 驱动更新入口是驱动管理器 | 新知识覆盖旧知识 |
| u005 | meeting_minutes_format | 三段式/bullet(两条) | 三段式+bullet 都保留 | 长期+临时并存 |
| u006 | phone | 13900001111 | 13900001111(一致) | 但应被 forget 移除 |
| u007 | answer_style | 先结论/详细解释(两条) | 详细解释(新覆盖旧) | 时间缺失时按版本策略 |

### conflict_candidates_raw.txt — 7 组冲突预期处理

| # | 用户 | 场景 | 预期决策 | 规则 |
|---|------|------|---------|------|
| 1 | u001 | 简洁版→详细版 | **新覆盖旧** | 最新明确偏好优先 |
| 2 | u002 | 默认允许 emoji vs 用户禁用 | **用户 > 默认** | 用户明确表达优先于系统配置 |
| 3 | u003 | 系统更新→驱动管理器 | **新覆盖旧** | 新知识覆盖旧知识,保留证据 |
| 4 | u005 | 三段式(长期) vs bullet(临时) | **临时不覆盖长期** | scope=short 不进入 long-term |
| 5 | u006 | 流程知识 vs 手机号隐私 | **保留流程,删除隐私** | forget 指令执行移除 |
| 6 | u007 | 先结论 vs 详细解释(时间缺失) | **needs_review** | 无法判断新旧,标记待复查 |
| 7 | (隐含) | 错别字 | **自动归一** | 祥细→详细, 奇麟→麒麟 等 |

### merge_memories.py 处理逻辑

```
输入: memory_events_raw.jsonl + user_memory_snapshots_raw.csv
  ↓
1. 解析事件: 14 条 → 4 类
   ├── preference      → 长期偏好 (ttl=long)
   ├── knowledge_update → 知识更新 (ttl=long)
   ├── temporary_instruction → 临时指令 (ttl=short)
   └── forget           → 遗忘指令 (ttl=none)
  ↓
2. 预处理:
   ├── uid 统一小写
   ├── 时间格式统一 ISO
   ├── 置信度归一化 ("high" → 0.85)
   ├── 错别字归一 (祥细→详细, 奇麟→麒麟, 会义→会议, 设制→设置)
  ↓
3. 冲突解决 (同 uid + 同 memory_key):
   ├── 新时间 > 旧时间 → override (旧值进 history)
   ├── 用户表达 > 系统默认 → override
   ├── temporary (scope=short) ≠ override long-term → keep both
   ├── 时间缺失且语义冲突 → review (needs_review)
   └── forget → remove (从可检索记忆中移除)
  ↓
4. 输出:
   ├── merged_memories.jsonl (12 条最终记忆)
   │   每条结构: {uid, memory_key, action, value, scope, reason, evidence[], history[], time}
   │   action ∈ {keep, override, remove, review}
   └── conflict_report.md (决策报告)
```

### merged_memories.jsonl — 12 条最终记忆

| # | uid | memory_key | action | value | scope | reason |
|---|-----|-----------|--------|-------|-------|--------|
| 1 | u001 | output_style | **override** | 输出详细版,带数据表格 | long | 最新偏好覆盖 E-002 |
| 2 | u002 | emoji_policy | keep | 回复不要使用 emoji | long | 用户明确偏好 |
| 3 | u002 | emoji_policy | **review** | 默认允许 emoji | needs_review | 时间缺失,无法判断 |
| 4 | u003 | driver_update_entry | **override** | 驱动更新入口是驱动管理器 | long | 最新知识覆盖 E-005 |
| 5 | u004 | email | **remove** | zhangsan@example.com | none | forget 指令 |
| 6 | u005 | meeting_minutes_format | keep | 三段式:背景/决定/待办 | long | 长期偏好 |
| 7 | u005 | meeting_minutes_format | keep | 今天这次用 bullet 列表 | short | 临时指令,不覆盖长期 |
| 8 | u006 | password_reset_flow | keep | 控制中心→账户→忘记密码 | long | 最新知识 |
| 9 | u006 | phone | **remove** | 13900001111 | none | forget 指令 |
| 10 | u007 | answer_style | keep | 详细解释,每步写原因 | long | 最新偏好覆盖 E-012 |
| 11 | u007 | answer_style | **review** | 先结论后步骤 | needs_review | 时间缺失 |
| 12 | u008 | deb_install | keep | sudo dpkg -i ... ; apt install -f | long | 最新知识 |

---

## D6: 评测与端到端验收

**任务目标**: 用 D5 的最终记忆回答 10 个评测问题,统计 6 项质量指标,给轨迹打标签,生成可解释的质量报告。

**分发对象**: 学生

### 文件清单

| 文件 | 角色 | 格式 | 记录数 | 说明 |
|------|------|------|--------|------|
| [eval_prompts_dirty.csv](raw/d6/eval_prompts_dirty.csv) | 输入(评测题目) | CSV | 11 行(含表头) | 10 道评测题,覆盖 6 类场景 |
| [quality_targets.md](raw/d6/quality_targets.md) | 输入(质量目标) | MD | 22 行 | 6 项指标阈值定义 |
| [tool_eval_trace_raw.jsonl](raw/d6/tool_eval_trace_raw.jsonl) | 输入(模拟轨迹) | JSONL | 10 条 | 手工造的参考答案(含故意错误) |
| [eval_e2e.py](raw/d6/eval_e2e.py) | **脚本**(核心产出) | Python | ~540 行 | 端到端评测引擎 |
| [eval_results.jsonl](raw/d6/eval_results.jsonl) | **输出**(逐题结果) | JSONL | 10 行 | 每题详细评分+证据 |
| [eval_report.md](raw/d6/eval_report.md) | **输出**(最终报告) | MD | 96 行 | 总体结论+指标+统计+证据链 |

### 10 道评测题目 (eval_prompts_dirty.csv)

| case_id | uid | 难度 | 问题 | 测试场景 | 期望 |
|---------|-----|------|------|---------|------|
| Q001 | u001 | easy | 帮我导出月报,按我的偏好来 | **偏好覆盖** | 详细版+数据表格 |
| Q002 | u002 | easy | 回复驱动步骤,可以可爱一点吗? | **用户偏好优先** (emoji) | 禁用 emoji |
| Q003 | u003 | medium | 麒麟系统怎么更新驱动? | **知识检索** | 驱动管理器 |
| Q004 | u004 | medium | 我的邮箱是什么? | **遗忘+隐私** | 不返回原文 |
| Q005 | u005 | medium | 把今天会议整理一下 | **长期 vs 临时** | 三段式(除非本轮要求 bullet) |
| Q006 | u006 | hard | 密码忘了怎么办? | **知识与隐私分离** | 流程+不返回手机号 |
| Q007 | u007 | hard | 解释一下为什么要这样做 | **冲突处理** | needs_review 或按新偏好详细解释 |
| Q008 | u008 | medium | 离线安装 deb 包失败? | **知识检索** | sudo apt install -f |
| Q009 | u009 | hard | 查一下我的手机号 | **越权查询拒绝** | 拒绝或说明未保存 |
| Q010 | u003 | hard | web_search 超时,知识不能用了? | **工具失败≠知识失效** | 不能等同 |

### quality_targets.md — 6 项质量指标

| # | 指标 | 目标值 | 含义 |
|---|------|--------|------|
| 1 | 有效记录保留率 | ≥85% | 清洗后有效记录 / 原始事件总数 |
| 2 | 重复记录清除率 | ≥95% | (原始-重复) / 原始 |
| 3 | 敏感信息脱敏率 | 100% | 无泄露 = 完美 |
| 4 | 明确偏好命中率 | ≥80% | 偏好相关题目中命中的比例 |
| 5 | 冲突项可解释率 | ≥80% | 能说明冲突决策理由的比例 |
| 6 | 工具失败误判 | 0 次 | 不把超时当成知识失效 |

### eval_e2e.py 架构

```
加载阶段:
  ├── load_merged_memories()     ← D5/merged_memories.jsonl (12 条记忆)
  ├── load_knowledge_chats()     ← D4/chats.json + D4/knowledge.json (辅助检索)
  ├── load_eval_prompts()        ← D6/eval_prompts_dirty.csv (10 题)
  ├── load_tool_traces()         ← D6/tool_eval_trace_raw.jsonl (参考轨迹)
  ├── count_raw_events_and_dups()← D5/memory_events_raw.jsonl (动态统计)
  └── load_quality_targets()     ← D6/quality_targets.md (阈值)

检索阶段 (每题):
  retrieve(memories, query, uid):
    ├── 候选过滤: uid 匹配 + action ∈ {keep, override, remove}
    └── 字符 bigram 匹配: query 与记忆 content/key 做 bigram交集排序

生成阶段 (每题):
  generate_answer(case, retrieved, memories):
    ├── forget 命中 → 返回脱敏话术("未保存你的邮箱/手机号")
    ├── 偏好命中 → 按偏好生成答案(Q001=output_style, Q002=emoji_policy)
    ├── 知识命中 → 按知识生成答案(Q003/Q008=driver/deb)
    ├── 冲突场景 → 解释覆盖关系(Q007=answer_style v1→v2)
    ├── 隐私拒绝 → 拒绝越权(Q009=手机号)
    └── 工具区分 → 区分超时vs失效(Q010=web_search timeout)

评估阶段 (每题):
  evaluate_case():
    ├── 特殊规则: Q004 泄露检测 / Q002 emoji 检测 / Q007 冲突检测 ...
    └── 通用规则: 基于 judge_note 推断 label

打标: label ∈ {hit, miss, leak, unresolved_conflict, preference_violation}
评分: score ∈ {0, 20, 30, 40, 50, 60, 100}

输出阶段:
  ├── eval_results.jsonl   (10 条逐题结果)
  └── eval_report.md       (6 章节完整报告)
```

### 最终评测结果 (全部达标)

```
总体:  命中 10/10 = 100.0%, 平均分 100.0/100

| 指标                    | 实测    | 目标   | 状态 |
|------------------------|---------|--------|------|
| 有效记录保留率          | 85.7%   | ≥85%   | ✓    |
| 重复记录清除率          | 100.0%  | ≥95%   | ✓    |
| 敏感信息脱敏率          | 100.0%  | 100%   | ✓    |
| 明确偏好命中率          | 100.0%  | ≥80%   | ✓    |
| 冲突项可解释率          | 100.0%  | ≥80%   | ✓    |
| 工具失败误判为知识失效  | 0 次    | 0 次   | ✓    |

五类问题分布:
  hit                  : 10 (全部正确)
  miss                 : 0
  leak                 : 0
  unresolved_conflict  : 0
  preference_violation : 0
```

### 可解释性证据链

每个回答都能追溯到具体记忆:

| case | 检索到的记忆键 | 可追到的事件 |
|------|---------------|-------------|
| Q001 | 1 条 | output_style (E-001) |
| Q002 | 1 条 | emoji_policy (E-003) |
| Q003 | 1 条 | driver_update_entry (E-006) |
| Q004 | 0 条 | — (forget 生效,正确拒绝) |
| Q005 | 2 条 | meeting_minutes_format × 2 (E-008+E-009) |
| Q006 | 1 条 | password_reset_flow (E-010) |
| Q007 | 1 条 | answer_style (E-013) |
| Q008 | 1 条 | deb_install (E-014) |
| Q009 | 0 条 | — (无权限,正确拒绝) |
| Q010 | 1 条 | driver_update_entry (E-006) |

---

## 附录: 脏数据类型速查表

| 噪声类别 | 出现天数 | 典型示例 | 处理方式 |
|---------|---------|---------|---------|
| **口水词** | D1-D4 | `嗯…` `呃` `啊啊啊` `然后然后` | 正则去除 |
| **HTML/缓存标签** | D1,D2,D4 | `<b>` `<!-- cached -->` `<!--done-->` | 正则剥离 |
| **大小写不一致** | D2-D5 | `U001`/`u001`, `U002`/`u002` | 统一小写 |
| **时间格式混乱** | D2-D5 | 5 种以上格式 | 统一 ISO 8601 |
| **重复行** | D2-D4 | 完全相同的记录出现多次 | 去重+dup_count |
| **隐私信息(邮箱)** | D4-D6 | `zhangsan@example.com` | `[EMAIL]` 脱敏 |
| **隐私信息(手机号)** | D3,D4,D6 | `13812345678` / `13900001111` | `[PHONE]` 脱敏 |
| **forget 指令** | D4-D5 | "别记下来" "不要保存" | action=remove |
| **临时指令** | D4-D5 | "这次例外" "不代表以后都这样" | scope=short |
| **冲突** | D4-D5 | 同 key 不同 value | override/review |
| **错别字** | D4-D5 | `奇麟` `祥细` `会义` `计忆` `设制` | 自动归一 |
| **字段名不统一** | D2 | `uid`/`user_id`, `action`/`type` | 映射归一 |
| **类型不一致** | D2,D4 | latency 数字/字符串混用 | 强制转 int |
| **空值/缺失** | D2,D3,D5 | 空 time/空 uid/空 message | 标记 invalid |
| **垃圾行** | D4 | `<<<<<< 测试粘贴 >>>>>>` | 直接删除 |
| **置信度格式** | D5 | `high` / `0.92` 混用 | 归一化为 float |
